// npm install @gluestack-ui/themed@1.1.34 @gluestack-style/react@1.0.57 @gluestack-ui/config@1.1.19 --legacy-peer-deps 

// npx expo install react-native-svg 

// import the config:
// import { config } from '@gluestack-ui/config'
// Wrap your application (or code inside App.tsx) around <GluestackUIProvider config={config}></GluestackUIProvider>

// to eject the gluestack UI theme and modify it later, code:
// npx gluestack-ui-scripts eject-theme
// then change it in the file config/gluestack-ui.config

// to use the changes made, change the imported config file route to './config/gluestack-ui.config'

// códigos http da série 100 são informativos
// Ex: 102 = processando

// 200 são de sucesso
// Ex: 200 = requisição bem sucedida
// Ex: 201 = criado com sucesso (geralmente porque usou POST)

// 300 são redirecionamento 
// Ex: 301 = movido permanentemente
// 302 movido

// 400 são erros do cliente
// Ex: 400 = Bad request (enviamos parametros errado ou esquecemos de enviar parametros necessários)
// Ex: 401 = Não autorizado (recurso na API que não temos autorização)
// Ex: 404 = Não encontrado (acessamos um recurso que não existe na API)

// 500 são erros no servidor
// Ex: 500 = erro interno

// ipconfig